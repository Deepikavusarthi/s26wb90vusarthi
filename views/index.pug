h1= title
p This page was updated for Assignment 10.
button(type="button", class="btn btn-secondary", onclick="reduce()") Sample Button
div(id="use") take some away

script.
  let current = 11;
  let quantity = 2;

  function reduce() {
    current = current - quantity;

    if (current < 0) {
      current = 0;
    }

    document.getElementById("use").textContent = `Reduced by ${quantity} to ${current}`;
  }

a(href="/zoos") Go to Zoos Search Results

